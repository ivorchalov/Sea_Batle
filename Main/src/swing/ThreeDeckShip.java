package swing;

import javafx.scene.Group;

public class ThreeDeckShip extends Ship{
    ThreeDeckShip(int health, int count) {
        super(health, count);
    }

    @Override
    public void draw(Group group, int x, int y) {
        super.draw(group, x, y);
    }
}
