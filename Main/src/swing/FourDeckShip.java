package swing;

import javafx.scene.Group;

public class FourDeckShip extends Ship{
    FourDeckShip(int health, int count) {
        super(health, count);
    }

    @Override
    public void draw(Group group, int x, int y) {
        super.draw(group, x, y);
    }
}
